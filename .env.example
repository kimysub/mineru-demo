# API Configuration
# Copy this file to .env and modify as needed

# Host address for the API server (inside container)
API_HOST=0.0.0.0

# Port for the API server (inside container)
API_PORT=8000

# Port exposed on the host machine
HOST_PORT=8000
